# 部署說明

## Cloudflare Pages

- Build command: (空)
- Build output directory: `/`
- 不需要環境變數

## 更新流程

1. 編輯 `../股票.xlsx`
2. `python rebuild_data.py` 重建 data.js
3. `git add . && git commit -m "更新" && git push`
4. Cloudflare 自動部署（1-2 分鐘）
